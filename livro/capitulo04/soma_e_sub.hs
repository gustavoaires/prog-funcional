soma_e_sub :: (Int, Int) -> (Int, Int)
soma_e_sub (a, b) = (a + b, a - b)

soma_e_sub_Currificada :: Int -> Int -> (Int, Int)
soma_e_sub_Currificada  a b = (a + b, a - b)

f_nao_currif (x, y, z, w) = x + y + z + w
f_currificada x y z w = x + y + z + w

